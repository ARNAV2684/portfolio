# Demo clips

Drop the PRIVATE project demo recordings here (referenced from `data/content.ts`):

- `sam-demo.mp4` + `sam-poster.jpg` — SAM Auto-Annotation
- `pipeline-demo.mp4` + `pipeline-poster.jpg` — ML Pipeline Platform

**Guidance (CLAUDE.md §7):** record ~10–15s, 16:9 or square, **muted**, compressed
H.264 MP4 (target < ~3–4 MB each). Export a representative **poster** still per clip.
A `.webm` source alongside the `.mp4` is a nice-to-have for smaller files.

Until these land, each video card shows a tasteful gradient placeholder with a
"▶ auto-playing" label — the layout already looks intentional.
